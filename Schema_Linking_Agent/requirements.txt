# Requirements for Query-Based Schema Filtering

# Local embedding model (sentence-transformers)
# Note: Groq doesn't have embeddings API, so we use local embeddings
sentence-transformers>=2.2.0

# Vector Database
chromadb>=0.4.0

# Utilities
numpy>=1.24.0
torch>=2.0.0  # Required by sentence-transformers

